#include<stdio.h>
#include<string.h>
int main()
	{
	char str1[100],str2[100];
	int i=0,len1,len2;
	printf("enter first string:");
	gets(str1);
	printf("enter second string:");
	gets(str2);

	len1=strlen(str1);
	len2=strlen(str2);

	for(i=0;i<=len2;i++)
	{
	str1[len1+i]=str2[i];
	}
	printf("%s",str1);
	return 0;
